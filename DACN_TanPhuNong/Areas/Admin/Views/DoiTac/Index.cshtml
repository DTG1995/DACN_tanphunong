@model IEnumerable<DACN_TanPhuNong.Models.tb_DoiTac>

@{
    ViewBag.Title = @GlobalRes.Partners;
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
<section class="content-header">
    <h1>
        @GlobalRes.Partners
        <small>@GlobalRes.List</small>
    </h1>
    <ol class="breadcrumb">
        <li><a href="@Url.Action("Index","UngDung")"><i class="fa fa-dashboard"></i> Quản trị</a></li>
        <li><a class="active">@GlobalRes.Partners</a></li>
    </ol>
</section>
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">@GlobalRes.List</h3>
                    <a href="@Url.Action("Create")" class="btn btn-primary" style="float: right">@GlobalRes.Add_new</a>
                </div>
                <div class="box-body">
                    <table id="datatable" class="table">
                        <thead>
                            <tr>
                                <th>
                                    @GlobalRes.Partners
                                </th>
                                <th>
                                    @GlobalRes.Images
                                </th>
                                <th class="no-sort"></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.TenDT)
                                    </td>
                                    <td>
                                        <img height="100px" src="@item.HinhAnh" />
                                    </td>
                                    <td>
                                        @Html.ActionLink(GlobalRes.Edit, "Edit", new { id = item.MaDT }) |
                                        @Html.ActionLink(GlobalRes.Detail, "Details", new { id = item.MaDT }) |
                                        @Html.ActionLink(GlobalRes.Delete, "Delete", new { id = item.MaDT })
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>

                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
    <!-- /.row -->
</section>
<!-- /.content -->
@Styles.Render("~/Content/admin/dataTables.bootstrap.min.css")
@Scripts.Render("~/Scripts/admin/jquery.dataTables.min.js")
@Scripts.Render("~/Scripts/admin/dataTables.bootstrap.min.js")
<script>
    $("#datatable").DataTable({
        columnDefs: [
        { targets: 'no-sort', orderable: false }
        ]
    });
</script>